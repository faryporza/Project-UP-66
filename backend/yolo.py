from ultralytics import YOLO

# 1. โหลดโมเดล
model = YOLO('best.pt')  # หรือ path ที่ไฟล์อยู่

# 2. สั่ง Export (สูตร Aimbot M2)
print("🚀 กำลังแปลงเป็น CoreML แบบความเร็วสูง...")

model.export(
    format='coreml', 
    nms=False,    # ⚠️ ต้องปิด! ให้ Python คำนวณเอง (แก้ปัญหากรอบหาย)
    half=True     # ✅ ต้องเปิด! ลดภาระเครื่อง M2 ให้ลื่นหัวแตก
)

print("✅ เรียบร้อย! ให้ดาวน์โหลดไฟล์ .mlpackage ตัวใหม่ไปใช้ครับ")